{
  "name": "hello-world-path",
  "version": "1.0.0",
  "description": "a simple hello world example with a path parameter passed from an environment variable",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "d3or",
  "license": "ISC",
  "dependencies": {
    "express": "^4.18.1"
  }
}
